import { redirect } from "next/navigation";

export default function AdminPage() {
  // Redirecionar diretamente para o dashboard CEO (autenticacao removida temporariamente)
  redirect("/lp-x7k9m2-internal/ceo");
}
